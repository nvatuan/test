Deploy from `staging` to `production` (<%= Time.now.localtime("+09:00") %>

### Deploying changes from `TEST-BASTION` to `PROD-BASTION`
<% pull_requests.each do |pr| -%>
  - [ ] confirmed? <%=  pr.to_checklist_item %>
<% end -%>
